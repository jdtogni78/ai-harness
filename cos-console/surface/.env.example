# surface has NO required secrets — it is fully local.
# Optional overrides:
# PORT=8787                # driver server port
# SURFACE_PROJECT=dstrader # initial project
# SURFACE_FIXTURE=1        # force fixture data (skip the live probe)
# SURFACE_URL=http://localhost:8787  # where drive.js posts intents
